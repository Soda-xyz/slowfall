# Deprecated: frontend/.dockerignore is deprecated.
# Use repository root's .dockerignore instead: /.dockerignore
# This file remains for backwards compatibility.

# Patterns to ignore when building with context 'frontend/' (if you do)
node_modules
dist
.env*
*.local
