# Activate dev profile for tests so DevBypassAuthFilter allows unauthenticated requests
spring.profiles.active=dev

# Tests run against embedded H2 ? disable Flyway (Postgres-specific migrations) for tests
spring.flyway.enabled=false

# Let Hibernate create/drop schema in the test lifecycle so repositories can operate
spring.jpa.hibernate.ddl-auto=create-drop
