# Example environment file for local testing (do NOT commit secrets)
# Copy to .env or use as a template and adapt values.

# Logstash destination (host and port)
LOGSTASH_HOST=localhost
LOGSTASH_PORT=5000

# Recommended Spring profile for production logging (TCP)
SPRING_PROFILES_ACTIVE=prod

